# Permit the NetGenix operator account to bind reverse SSH forwards to the
# private Docker bridge. The edge client supplies the exact bridge address;
# no listener is bound to the VM's public or LAN interfaces.
Match User fmboma
    GatewayPorts clientspecified
